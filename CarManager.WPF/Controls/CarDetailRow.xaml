<UserControl x:Class="CarManager.WPF.Controls.CarDetailRow"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarManager.WPF.Controls"
             mc:Ignorable="d"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             d:DesignHeight="50" d:DesignWidth="100"
             d:FontFamily="{StaticResource Roboto}">
    
    <Border Padding="5 10"
            BorderThickness="0 0 0 1"
            BorderBrush="{StaticResource Brush.Border}">

        <Border>

            <Grid VerticalAlignment="Center">
                
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                
                <!-- Label -->
                <TextBlock Grid.Column="0" 
                           Text="{Binding Label,ElementName=root, FallbackValue=Label}"
                           FontSize="12"
                           Foreground="{StaticResource Brush.Text.Secondary }"
                           VerticalAlignment="Center"/>

                <!-- Value -->
                <TextBlock Grid.Column="1"
                           FontSize="15"
                           Text="{Binding Value, ElementName=root, FallbackValue=Value}"
                           VerticalAlignment="Center"
                           FontWeight="Medium"/>

                
                
                
            </Grid>
            
        </Border>
        
    </Border>
    
</UserControl>
